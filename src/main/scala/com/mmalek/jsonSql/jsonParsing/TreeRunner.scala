package com.mmalek.jsonSql.jsonParsing

import com.mmalek.jsonSql.jsonParsing.dataStructures.{JNull, JValue, JsonCreatorsTree}

object TreeRunner {
  def run(tree: JsonCreatorsTree): JValue = {
    JNull(0)
  }
}
